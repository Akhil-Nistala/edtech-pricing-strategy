# Core analysis
pandas>=2.2
numpy>=1.26
scipy>=1.11

# ML layer: churn-risk, segment-inference, and conversion-propensity models
scikit-learn>=1.4

# Optional: only needed if loading data into MySQL from Python instead of
# `mysql -u ... < sql/01_load_data.sql`
mysql-connector-python>=8.0
